<Window x:Class="RIATCClient.ConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect"
        Width="400" Height="220"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#1A1A1A"
        BorderBrush="#3A3A3A"
        BorderThickness="1">

    <Border BorderBrush="#3A3A3A" BorderThickness="1" Padding="20" Background="#1A1A1A">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Connect to RIATC Network"
                       Foreground="#EAEAEA"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,15"
                       Grid.Row="0"/>

            <!-- Input fields -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Callsign:" Foreground="#EAEAEA" Margin="0,0,0,4"/>
                    <TextBox x:Name="CallsignBox"
                             Background="#121212"
                             Foreground="#EAEAEA"
                             BorderBrush="#3A3A3A"
                             BorderThickness="1"
                             Height="26"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Type Code:" Foreground="#EAEAEA" Margin="0,0,0,4"/>
                    <TextBox x:Name="TypeBox"
                             Background="#121212"
                             Foreground="#EAEAEA"
                             BorderBrush="#3A3A3A"
                             BorderThickness="1"
                             Height="26"/>
                </StackPanel>
            </Grid>

            <!-- Checkbox -->
            <CheckBox x:Name="ObserverCheck"
                      Grid.Row="2"
                      Content="Connect in shared cockpit mode (observer mode)"
                      Foreground="#EAEAEA"
                      Margin="0,5,0,10"/>

            <!-- Divider line -->
            <Border Grid.Row="3"
                    Background="#303030"
                    Height="1"
                    Margin="0,10,0,15"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Connect"
                        Width="100"
                        Height="28"
                        Background="#2B2B2B"
                        Foreground="#EAEAEA"
                        BorderBrush="#3A3A3A"
                        BorderThickness="1"
                        Click="Connect_Click"/>
                <Button Content="Cancel"
                        Width="80"
                        Height="28"
                        Background="#2B2B2B"
                        Foreground="#EAEAEA"
                        BorderBrush="#3A3A3A"
                        BorderThickness="1"
                        Margin="10,0,0,0"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
